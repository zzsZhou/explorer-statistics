<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.ontio.explorer.statistics.mapper.NodePositionHistoryMapper">
    <resultMap id="BaseResultMap" type="com.github.ontio.explorer.statistics.model.NodePositionHistory">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="public_key" jdbcType="VARCHAR" property="publicKey"/>
        <id column="block_height" jdbcType="BIGINT" property="blockHeight"/>
        <result column="address" jdbcType="VARCHAR" property="address"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="node_rank" jdbcType="INTEGER" property="nodeRank"/>
    </resultMap>
    <!--
      self-defined SQL
    -->
    <select id="selectLatestBlockHeight" resultType="java.lang.Long" useCache="false">
        SELECT block_height
        FROM tbl_node_position_history
        ORDER BY block_height DESC
        LIMIT 1;
    </select>
    <insert id="batchInsertSelective" parameterType="java.util.List">
        insert into tbl_node_position_history (public_key, address, name, node_rank, block_height)
        values
        <foreach collection="list" index="index" item="item" separator=",">
            (#{item.publicKey,jdbcType=VARCHAR}, #{item.address,jdbcType=VARCHAR}, #{item.name,jdbcType=VARCHAR},
            #{item.nodeRank,jdbcType=INTEGER},#{item.blockHeight,jdbcType=BIGINT})
        </foreach>
    </insert>
</mapper>